import { useState, useEffect } from 'react';
import styled from 'styled-components';

export default function EditRecipe({ recipe: data }) {
    const [recipe, setRecipe] = useState(data);

    return (
        <div>
            
        </div>
    );
}
